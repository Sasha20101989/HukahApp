# PostgreSQL

PostgreSQL is included in `infrastructure/docker-compose.yml`.

Next backend step:

- Add EF Core persistence per service.
- Add migrations.
- Keep service schemas separate.
- Publish integration events instead of sharing tables across services.
